[project]
name = "Google Web Vitals frontend performance (throughline source)"
format_version = 3

# A throughline *source*: a standalone graph a consuming project composes with
# `tl`, importing it under a namespace (e.g. `webvitals`) and referencing
# rules as `webvitals:SR-0001`. The UIDs here are this source's own and immutable; the
# published guide reference lives in `attrs.source_ref`, never in the UID.
#
# This source re-expresses Google's Web Vitals / web.dev performance guidance: each Core Web Vital and optimisation area is a user_requirement, and every individual recommendation is a system_requirement that implements its section.
#
# Edition handling: style guides are living documents. A material revision is cut as
# a dated tag on this repo (e.g. v2026-08); a consumer pins the ref it wants.

[grounding]
root_types = ["intent"]
delivery_roots = ["intent"]
ground_link_types = ["derives_from", "implements"]
ai_origins = ["ai", "hybrid"]

[types.user_requirement]
attrs.source_ref = { required = true }

# This guide grades no rule by level, so a system_requirement requires only its
# source reference.
[types.system_requirement]
attrs.source_ref = { required = true }

[links]
types = ["derives_from", "implements", "relates"]

[link_rules]
derives_from = { from = ["user_requirement"], to = ["intent"] }
implements   = { from = ["system_requirement"], to = ["user_requirement"] }

[status]
values = ["proposed", "draft", "deferred", "approved", "ratified", "implemented",
          "verified", "suspect", "rejected", "deleted"]

[transitions]
proposed    = ["draft", "approved", "ratified", "deferred", "suspect", "rejected", "deleted"]
draft       = ["approved", "deferred", "suspect", "rejected", "deleted"]
deferred    = ["draft", "approved", "suspect", "rejected", "deleted"]
approved    = ["ratified", "implemented", "deferred", "suspect", "rejected", "deleted"]
ratified    = ["implemented", "suspect", "rejected", "deleted"]
implemented = ["verified", "suspect", "rejected", "deleted"]
verified    = ["implemented", "suspect", "deleted"]
suspect     = ["approved", "ratified", "implemented", "verified", "rejected", "deleted"]
rejected    = ["draft", "deleted"]

[docs]
paths = ["docs/*.md", "README.md"]

[status.roles]
initial = "draft"
proposed = "proposed"
ratified = "ratified"
invalidated = "rejected"
suspect = "suspect"
tombstone = "deleted"
